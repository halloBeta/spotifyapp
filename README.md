# Spotify App
- This is a Java Spring Boot application that uses specific Spotify endpoints to get data about genres and new-releases.
- Exposes to RESTful apis

## Local setup in Windows
- ./mvnw clean install/compile
- ./mvnw spring-boot:run
- Note: to clean up the workspace, do ctrl + shift + p, then on windows palatte run java: clean workspace